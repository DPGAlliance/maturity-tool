# Data Viewer

Commands to run the app:
- `streamlit run data_viewer/data_viewer/main.py`

The viewer uses the database cache by default.

GitHub auth:
- Streamlit secrets: `GITHUB_TOKEN`
- Or env / Docker secrets: `GITHUB_TOKEN` or `GITHUB_TOKEN_FILE`